{
  "name": "goelectricnew-root",
  "version": "1.0.0",
  "description": "Root package for Goelectricnew",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run start --prefix backend\" \"npm run preview --prefix client\"",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix client\"",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix client",
    "build:client": "npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
